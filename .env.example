VITE_MANIFEST_URL=
VITE_AUDIO_LOAD_TIMEOUT_MS=20000
VITE_PROGRESS_UPDATE_HZ=4
VITE_SOURCE_RADIUS_M=
# Pre-fills the preflight latitude/longitude fields (set both together, or neither).
# e.g. 37.5665 / 126.978 to match the bundled Seoul content fixtures.
VITE_DEFAULT_LAT=
VITE_DEFAULT_LON=
VITE_BUILD_COMMIT=dev
